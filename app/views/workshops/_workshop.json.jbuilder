json.extract! workshop, :id, :name, :category, :city, :date, :time, :length, :max_partecipants, :price, :location, :description, :created_at, :updated_at
json.url workshop_url(workshop, format: :json)
